"""Full inference pipeline: text + object → interaction latent → motion.

Usage:
    piano-generate --text "pick up the box" --object box.ply --output motion.npy
"""
from __future__ import annotations


def main() -> None:
    """CLI entrypoint for ``piano-generate``."""
    raise NotImplementedError("Inference not yet implemented")


if __name__ == "__main__":
    main()
